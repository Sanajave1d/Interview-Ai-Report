{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "PUPPETEER_CACHE_DIR=/app/.cache/puppeteer npm install --omit=dev && PUPPETEER_CACHE_DIR=/app/.cache/puppeteer npx puppeteer browsers install chrome"
  },
  "deploy": {
    "startCommand": "PUPPETEER_CACHE_DIR=/app/.cache/puppeteer npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}